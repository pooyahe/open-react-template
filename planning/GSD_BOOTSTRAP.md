# GSD Bootstrap for Existing Template

This is an existing-codebase adaptation, not a greenfield bootstrap.

1. Install GSD Core for Codex at project/local scope.
2. Initialize only if `.planning/PROJECT.md` is absent.
3. Seed the product context from `docs/PRD.md`.
4. Reconcile the generated roadmap with `TASK.md`.
5. Ensure the first implementation phase is **Template audit and adaptation foundation**.
6. Preserve generated GSD state; do not replace it with this file.
7. Update state after each verified phase and atomic commit.
