# Project Skills Index

Codex discovers the actual skills under `.agents/skills/`. This file is a human-readable index only.

| Skill | Use |
|---|---|
| `project-planning` | Reconcile the PRD, TASK phases, GSD artifacts, and decisions. |
| `website-foundation` | Initialize and structure the Next.js application consistently. |
| `cinematic-scroll` | Implement the paper-to-data hero with performance and accessibility fallbacks. |
| `content-seo` | Apply approved German copy, metadata, structured data, and honest claims. |
| `quality-gate` | Run deterministic checks, browser QA, accessibility review, and evidence-based completion. |

Invoke explicitly in Codex with `$skill-name` when useful. Codex may also activate a skill when the task matches its description.
